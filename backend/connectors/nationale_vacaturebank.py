import requests

def fetch_jobs(keywords: str = "data analyst", location: str = "") -> list:
    """Fetch jobs from Nationale Vacaturebank (Netherlands)
    NOTE: API endpoint needs further investigation - temporarily disabled
    """
    return []